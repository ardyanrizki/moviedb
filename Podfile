# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'movieapi_project' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	pod 'Kingfisher'
pod 'youtube-ios-player-helper'

  # Pods for movieapi_project

  target 'movieapi_projectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'movieapi_projectUITests' do
    # Pods for testing
  end

end
